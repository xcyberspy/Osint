# Osint
OSINT Analyzer – A Flask-based OSINT web tool that scrapes DuckDuckGo search results, classifies sources (social media, news, code repos, official sites), and visualizes relationships using an interactive graph.
